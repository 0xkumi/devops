pipeline {
    agent {label 'docker'}
    stages {	
        stage('Run another job') {
            steps {
                build(job: "Build_IncognitoTestnet", wait: true, parameters: "20190722_4")
            }
            
        }
    }
}